{
	"name": "nabu-namer",
	"version": "0.1.0",
	"description": "Public brand-name generator — a free tool from Nabu",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "wrangler pages dev",
		"deploy": "wrangler pages deploy"
	}
}
